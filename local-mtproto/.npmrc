# Skip ws's OPTIONAL native deps (bufferutil / utf-8-validate). They're a perf-only
# tweak and require a C toolchain + Python to build (which Termux/minimal hosts lack).
# GramJS/ws work fine without them, so we omit them and avoid node-gyp build failures.
omit=optional
