{
  "name": "dealverse",
  "version": "0.1.0",
  "bin": {
    "dealverse": "bin/dealverse.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "fe:install": "npm --prefix frontend install",
    "fe:build": "npm --prefix frontend run build && node scripts/copy-fe.js",
    "lambda:install": "node scripts/package-lambda.js",
    "deploy": "npm run fe:build && npm run lambda:install && cdk deploy --require-approval never",
    "deploy:infra": "npm run lambda:install && cdk deploy --require-approval never",
    "destroy": "cdk destroy"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "aws-cdk": "2.1138.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.265.0",
    "constructs": "^10.3.0"
  }
}
