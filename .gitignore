node_modules/
cdk.out/

# CDK / TS build artifacts (scoped — do NOT use a blanket *.js rule)
*.d.ts
bin/*.js
lib/*.js
*.js.map

# CDK local
.cdk.staging/
cdk.context.json

# Frontend build + deps (regenerated by npm run fe:build)
frontend/node_modules/
frontend/dist/
lambda/public/

# Lambda runtime deps (regenerated by npm run lambda:install before deploy)
lambda/node_modules/

# OS / editor
.DS_Store

# Local screenshots (but keep brand assets like dealverse.png)
Capture*.PNG
Capture*.png

# Built APKs
*.apk

# Dropped screenshots / reference images (not brand assets like dealverse.png)
Capture*.jpg
*.jfif
WhatsApp Image*
