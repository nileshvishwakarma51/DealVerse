{
  "name": "dealverse-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "DealVerse Lambda runtime dependencies. The @aws-sdk/* packages are provided by the Node.js Lambda runtime and are intentionally NOT listed here. Only the MTProto (beta) feature needs a bundled dependency: GramJS (telegram).",
  "dependencies": {
    "telegram": "2.26.22"
  }
}
